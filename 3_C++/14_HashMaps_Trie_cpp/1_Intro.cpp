// Hash map is a data structure in which insertion,deletion and searching takes place in O(1) complexity using
//  hashtable and unordered maps

// Can be used in problems like counting number of max occuring element(character/word) in string
// Other mapping applications can also be performed with this.