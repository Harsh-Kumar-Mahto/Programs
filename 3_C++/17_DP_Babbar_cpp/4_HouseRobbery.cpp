// There are n given number of houses in a localty that are located in a cicular fashion
// We have to find the maximum amount of money that can be robbed from the houses
// If two adjacent houses are robbed then the police will catch us.

#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int>v(n);
    for(int i = 0; i < n; i++){
        cin >> v[i];
    }
    
}